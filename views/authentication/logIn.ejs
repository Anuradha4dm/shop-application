<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/forms.css">
    <link rel="stylesheet" href="/css/product.css">
</head>

<body>
   <%- include('../includes/navigation.ejs') %>

    <main>
            <% if (messege) { %>
                <p class="err-msg"><%= messege %> </p>
             
            <% } %>

        <form class="product-form" action="/log-in" method="POST">
            
            <div class="form-control">
                <label for="username">Username or Email</label>
                <input type="email" name="email" id="username" value="<%= oldInput.email %>" >
            </div>
            <div class="form-control">
                <label for="password">Password</label>
                <input type="password" name="password" id="password"  value="<%= oldInput.password %>">
            </div>
          
            <input type="hidden" name='_csrf' value="<%= csrfNumbrt %>" >
            <button class="btn" type="submit">Log In</button>
            <a class="passwordforget" href="/password-reset">Foget password?</a>
        </form>
    </main>
<%- include('../includes/end.ejs') %>